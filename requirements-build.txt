# PyInstallerビルド用依存関係

# 基本的なビルドツール
PyInstaller>=5.0
setuptools>=45.0
wheel>=0.37.0

# ビルドプロセス用
pathlib2>=2.3.0; python_version<"3.6"

# Windows実行ファイル作成用（Windows環境のみ）
pywin32>=227; sys_platform=="win32"

# 追加の依存関係（実行時に必要）
pandas>=1.3.0
openpyxl>=3.0.9
click>=8.0.0
pydantic>=1.8.0
python-dateutil>=2.8.0
pyyaml>=6.0
tqdm>=4.60.0
pillow>=8.0.0